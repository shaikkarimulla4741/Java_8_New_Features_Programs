package LambdaExpressions;

public interface FirstInterface {
      void firstInter() ;

    /* This is a Functional Interface, because we have only 1 method inside
    * Interface.
    * We can use the Lambda Expressions only on Functional Interfaces.
    * */
}
