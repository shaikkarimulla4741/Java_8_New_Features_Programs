package MethodAndConstructorRef.MethodRef;

@FunctionalInterface
public interface WorkInterface {
     void doTask();
//     If we want to create return type we need to have return type in Class  also
}
